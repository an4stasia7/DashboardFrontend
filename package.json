{
  "name": "dashboard-desktop",
  "version": "1.2.5",
  "description": "Управленческий дашборд — desktop (Electron)",
  "main": "main.js",
  "author": "Dashboard",
  "scripts": {
    "start": "electron .",
    "prepare-release-config": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/prepare-release-config.ps1",
    "restore-dev-config": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/restore-dev-config.ps1",
    "dist": "npm run prepare-release-config && electron-builder --win && npm run restore-dev-config",
    "dist:dir": "npm run prepare-release-config && electron-builder --win --dir && npm run restore-dev-config",
    "dist:publish": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-dist-publish.ps1",
    "dist:publish:raw": "npm run prepare-release-config && electron-builder --win --publish always && npm run restore-dev-config"
  },
  "build": {
    "appId": "ru.dashboard.desktop",
    "productName": "Управленческий дашборд",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "*.html",
      "css/**/*",
      "js/**/*",
      "temp/**/*",
      "node_modules/katex/dist/katex.min.css",
      "node_modules/katex/dist/katex.min.js",
      "node_modules/katex/dist/fonts/**/*",
      "package.json"
    ],
    "asar": true,
    "publish": [
      {
        "provider": "github",
        "owner": "an4stasia7",
        "repo": "DashboardFrontend",
        "releaseType": "release"
      }
    ],
    "win": {
      "signAndEditExecutable": false,
      "executableName": "DashboardDesktop",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "artifactName": "dashboard-desktop-setup-${version}.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false,
      "unicode": true,
      "useZip": true,
      "warningsAsErrors": false,
      "differentialPackage": false,
      "shortcutName": "Управленческий дашборд"
    },
    "portable": {
      "artifactName": "dashboard-desktop-portable-${version}.${ext}"
    }
  },
  "devDependencies": {
    "electron": "^33.3.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "katex": "^0.16.21"
  }
}
